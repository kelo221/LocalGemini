# Local Gemini
Vibe coded local Gemini LLM client

Add key from https://aistudio.google.com/api-keys

Run a development build:

```shell
bun run tauri dev
```

Build the program:

```shell
bun run tauri build
```
